<table class="table">
  <thead>
    <tr>
      <th scope="col">Title</th>
      <th scope="col">Body</th>
      <th scope="col">Acton</th>
    </tr>
  </thead>
  <tbody>
    <% @article.comments.each do |comment| %>
      <tr>
        <td><%= comment.title %></td>
        <td><%= comment.body %></td>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
